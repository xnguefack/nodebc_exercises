// Create a new index25.js file
// We'll define a super hero character using variables and values
// Use the right data type for each variable
const superHeroName = "Batman"; // Variable name: superHeroName, value: Batman
const name = "Bruce Wayne"; // Variable name: name, value: Bruce Wayne
let fly = false; // Variable name: fly, value: false
let hasBatmobile = true; // Variable name: hasBatmobile, value: true
let life = 36; // Variable name: life, value: (value between 0 and 100)
let freeTime = null; // Variable name: freeTime, value: null
// Show each value using console.log() with the variable name (string) as first parameter and the actual variable value as second
// Example: console.log('name', name);
console.log("\n");
console.log("Super hero name:", superHeroName, `| the type of variable here is a ${typeof superHeroName}.`);
console.log("Real name:", name, `| the type of variable here is a ${typeof superHeroName}.`);
console.log("No aid fly capability:", fly, `| the type of variable here is a ${typeof fly}.`)
console.log("Has a Batmobile:", hasBatmobile, `| the type of variable here is a ${typeof hasBatmobile}.`)
console.log("Approximative known age:", life, `| the type of variable here is a ${typeof life}.`)
console.log("Free time:", freeTime, `| the type of variable here is a ${typeof freeTime}.`)
    // Also show the variable names and the type of value it has assigned (use typeof)